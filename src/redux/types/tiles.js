import { createType } from './helpers';

export const SET = createType('TILES/SET');
export const RESET = createType('TILES/RESET');
export const CLICK = createType('TILES/CLICK');
export const RUN = createType('TILES/RUN');
